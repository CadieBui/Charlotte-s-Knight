using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class totalTool : MonoBehaviour
{
    public int tool_num;
    public Image[] border;

    // Start is called before the first frame update
    void Start()
    {
        tool_num=0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
